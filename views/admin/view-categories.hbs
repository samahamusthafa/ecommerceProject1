
          <div class="content-wrapper">
            <!-- Content -->

            <section>
    <div class="container mt-5">

        <div class="card">
            <h5 class="card-header">E Shopper</h5>
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Image</th>
                            <th>Category</th>
                           
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                        {{#each categories}}
                        <tr>
                            <td>{{inc @index}}</td>
                            <td><img style="width:50px; height:50px;" src="/category-images/{{path}}" alt="" />
                            </td>
                            <td>{{this.category}}</td>
                            
                            {{!-- <td>Rs.{{this.price}}</td> --}}
                            {{!-- <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>Angular
                                    Project</strong></td>
                            <td>Albert Cook</td>
                            <td></td>
                            <td><span class="badge bg-label-primary me-1">Active</span></td> --}}
                            <td>
                                <div class="dropdown">
                                    <button type="button" class="btn p-0 dropdown-toggle hide-arrow"
                                        data-bs-toggle="dropdown">
                                        <i class="bx bx-dots-vertical-rounded"></i>
                                    </button>
                                    <div class="dropdown-menu">
                                        <a href="/admin/edit-category/{{this._id}}" class="dropdown-item"><i
                                                class="bx bx-edit-alt me-1"></i>
                                            Edit</a>
                                        {{!-- <a href="/admin/delete-category/{{this._id}}" class="dropdown-item"
                                            onclick="return confirm('Are you want to delete {{this.name}} ?')"><i
                                                class="bx bx-trash me-1"></i>
                                            Delete</a> --}}
                                            <a class="dropdown-item" onclick="return deleteCategory('{{this._id}}')"><i
                                class="bx bx-trash me-1"></i>
                              Delete</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                        {{/each}}


                    </tbody>
                </table>
            </div>
        </div>


    </div>
</section>
                <!--/ Total Revenue -->
                

<script>
          function deleteCategory(catId) {
            Swal.fire({
              title: 'Are you sure?',
              text: "You won't be able to revert this!",
              icon: 'warning',
              showButton: true,
              confirmButtonColor: '#3085d6',
              ButtonColor: '#d33',
              confirmButtonText: 'Yes, delete it!'
            }).then((result) => {
              if (result.isConfirmed) {
                $.ajax({
                  url: "/admin/delete-category/" + catId,
                  success: () => {
                      Swal.fire(
                      'Deleted!',
                      'Your file has been deleted.',
                      'success'
                    )

                  }


                })

              }
            })
          }
        </script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>



